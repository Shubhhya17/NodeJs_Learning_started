// const lib = require('./librery.js'); // Ensure the file name matches exactly

// const res = lib.add(10, 2);
// console.log(res); // This should print 3
 

    const hello = require('./   librery.js');

    const res = hello.add(10,2);
    console.log(res);